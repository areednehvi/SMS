<Window x:Class="SMS.Views.combo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMS.Views"
        mc:Ignorable="d"
        Title="combo" Height="300" Width="300">
    <Window.Resources>
        <local:MainWindowVM x:Key="MainWindowVM" />
    </Window.Resources>
    <Grid DataContext="{StaticResource MainWindowVM}">
        <ComboBox
    IsEditable="True"
    IsReadOnly="True"
    ItemsSource="{Binding Items}"
    Text="{Binding Text}">
            <ComboBox.ItemTemplate>
                <DataTemplate
            DataType="{x:Type local:Item}">
                    <CheckBox
                Content="{Binding Name}"
                IsChecked="{Binding IsChecked}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</Window>
